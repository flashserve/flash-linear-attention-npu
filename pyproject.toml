[build-system]
requires = ["setuptools>=70.1", "wheel", "packaging", "psutil"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
    "npu: tests that require an available Ascend NPU",
]
